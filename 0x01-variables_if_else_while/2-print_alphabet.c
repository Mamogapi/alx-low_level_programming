#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Sucess)
*/
 
int main(void)
{
	char alph;

 	for (alph = 'a' ;alph <= 'z' ; alpha++)
	{
		putchar(alph);
	} 
	
	putchar('\n');
	return (0);
}


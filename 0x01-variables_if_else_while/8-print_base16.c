#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Succes)
*/

int main(void)
{
	int x;

	for (x = 0 ; x < 16 ; x++)
	{
		if(n < 10)
		{
			putchar('0' + n);
		}
		else
		{
			putchar(87 + n);
		}

	}
	putchar('\n');
	return (0);

}

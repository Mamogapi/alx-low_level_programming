#include <stdio.h>
#include <string.h>

/**
 * _strlen -  returns the length of a string
 * @s: charactor variable
 * Return: integer value
 */

int _strlen(char *s)
{
	int x;

	x = strlen(*s);

	return (x);
}
